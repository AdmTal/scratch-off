// Test that the scratch-off package can be imported from NPM
import { ScratchOff } from 'scratch-off';

console.log('scratch-off package loaded successfully from NPM!');
console.log('ScratchOff class:', ScratchOff);

// The ScratchOff class auto-initializes when imported,
// but we can also manually create instances if needed
